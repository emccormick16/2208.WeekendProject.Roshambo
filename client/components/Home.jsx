import React from "react";

const homepageStyles = {
  backgroundColor: "dodgerBlue",
  textDecoration: "underline",
};

const Home = () => {
  return <h1 style={homepageStyles}>Homepage</h1>;
};

export default Home;
